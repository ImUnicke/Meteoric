if (Meteor.isClient) {
  //This file is useless!!!!
  // Whatever keep it there!!
  // ok then!!!
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
